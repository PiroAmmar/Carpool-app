NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

ALLOWED_EMAIL_DOMAIN=nu.edu.pk

# Resend — using their test domain (onboarding@resend.dev) until a real domain is verified.
# Swap RESEND_FROM_EMAIL once nu.edu.pk (or a subdomain) is verified in the Resend dashboard.
RESEND_API_KEY=
RESEND_FROM_EMAIL="Carpool Hub <onboarding@resend.dev>"
ADMIN_EMAIL=piroammar388@gmail.com

# Vercel Cron auth — Vercel sets this header automatically in production.
# Set the same value here for local testing of /api/cron/digest.
CRON_SECRET=
